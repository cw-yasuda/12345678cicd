name: Reuse Workflow

on:
  workflow_call:

jobs:
  greeting:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Hello World
        run: |
          echo "Hello World!"